type customizationObject = {
    primary: string;
    secondary: string;
    background: string;
    backgroundDark: string;
    success : string
    error : string
};

export default customizationObject;